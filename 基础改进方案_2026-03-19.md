# 群约小助手｜基础改进方案（可直接落地）

日期：2026-03-19  
适用范围：当前仓库（Flask + SQLite + 原生前端）

## 0. 当前基线（已确认）

- 功能可用：创建、加入、填写、汇总、分享、AI 总结、备注已打通。
- 工程基础：前端已拆分，具备 `/healthz`。
- 安全现状：已改用环境变量，但历史密钥风险待收口。
- 运行状态：本地可启动（`run.py` 可正常拉起服务）。

### 执行状态（2026-03-19）
- A1 密钥轮换：**已完成（旧 key 已删除/失效）**
- A2 历史治理：**未执行历史改写**（按当前策略：先不删历史，仅持续扫描与防误提）
- B1 冒烟测试：**已完成**（已新增 [tests/test_api_smoke.py](tests/test_api_smoke.py)）
- B2 发布门禁：**已完成**（README 已加入发布前检查清单）
- C1 结构化日志：**已完成（2026-03-29）**
- C2 备份恢复 SOP：**已完成文档落地（2026-03-29，待补演练记录）**
- D1 AI 降级文案：**已完成（2026-03-29）**

---

## 1. 目标与优先级（两周内）

### P0（本周必须完成）
1. 密钥风险收口（轮换 + 历史治理策略确定）
2. 增加最小自动化冒烟测试（核心 4 条链路）
3. 建立发布前检查清单（可执行、可复用）

### P1（下周完成）
1. 日志规范化（错误可追踪）
2. 备份恢复 SOP（可演练）
3. AI 降级文案（无 Key 时可读输出）

---

## 2. 可直接执行的改进清单

## A. 安全收口（优先级最高）

### A1. 密钥轮换（当天完成）
- 在 DeepSeek 控制台生成新 Key、作废旧 Key。
- 更新部署平台变量与本机用户变量（不写入仓库）。
- 验证：启动后 AI 总结可用，旧 Key 不可用。

验收标准：
- 仓库当前分支无真实 Key。
- 线上与本地均使用新 Key。

### A2. 历史泄露治理（1 天）
- 评估是否需要重写 Git 历史（取决于仓库公开性与传播范围）。
- 若执行历史清理：完成后强制推送，并通知协作者重新克隆。
- 在 README 增加“密钥轮换与泄露应急”小节。

验收标准：
- 历史扫描不再命中真实密钥模式。
- 团队成员知晓新的拉取方式与风险说明。

---

## B. 测试与发布门禁（1~2 天）

### B1. 新增最小 API 冒烟测试
建议新增文件：
- [tests/test_api_smoke.py](tests/test_api_smoke.py)

覆盖 4 条链路：
1. 创建会话：`POST /api/session`
2. 加入会话：`POST /api/session/<sid>/join`
3. 提交可用性+备注：`PUT /api/session/<sid>/avail`
4. 拉取会话：`GET /api/session/<sid>`

验收标准：
- 4 条用例全部通过。
- 测试可在本地一条命令执行。

### B2. 发布前检查清单（README 落地）
在 [README.md](README.md) 增加“发布前检查”章节：
- 测试通过
- `/healthz` 返回 200
- 环境变量配置正确
- 会话创建/加入/备注/总结各手测 1 次

验收标准：
- 新人按清单可独立完成发布前自检。

---

## C. 运维可维护性（1~2 天）

### C1. 统一日志结构
建议改动文件：
- [server.py](server.py)

动作：
- 增加请求级日志：方法、路径、状态码、耗时。
- 异常日志统一包含：错误码、请求 ID、简要上下文。

验收标准：
- 关键 API 出错时可在日志中定位到对应请求。

### C2. 备份恢复 SOP
建议新增文件：
- [docs/backup_restore.md](docs/backup_restore.md)

动作：
- 明确 `sessions/sessions.db` 的备份频率、备份位置、恢复步骤。
- 提供一次演练记录模板（时间、操作人、恢复结果）。

验收标准：
- 按文档可在 15 分钟内完成一次恢复演练。

---

## D. 体验兜底（0.5 天）

### D1. AI 降级文案
建议改动文件：
- [server.py](server.py)
- [app.js](app.js)

动作：
- 无 `DEEPSEEK_API_KEY` 时，返回“规则版总结”（非报错堆栈）。
- 前端提示“当前为基础总结模式”。

验收标准：
- 关闭 Key 后，用户仍能获得可读总结。

---

## 3. 两周排期（单人可执行）

- Day 1：A1 + A2（至少完成轮换与策略确认）
- Day 2：B1（测试文件）
- Day 3：B2（发布清单）+ 回归
- Day 4：C1（日志）
- Day 5：C2（SOP）+ 演练
- Day 6：D1（AI 降级）
- Day 7：全量回归 + 文档收口

---

## 4. 本轮完成后的预期结果

- 完整度可从 78/100 提升到 86~90/100。
- 安全完整度可从 65/100 提升到 80+/100。
- 工程完整度可从 70/100 提升到 82+/100。
- 运维完整度可从 72/100 提升到 82+/100。

---

## 5. 最小交付物清单（可打勾）

- [x] 新 Key 已轮换且旧 Key 已失效
- [x] 历史泄露治理策略已执行/书面确认（当前策略：不改历史，持续扫描）
- [x] [tests/test_api_smoke.py](tests/test_api_smoke.py) 已新增并通过
- [x] [README.md](README.md) 已加入发布前检查清单
- [x] [server.py](server.py) 已加入结构化日志
- [x] [docs/backup_restore.md](docs/backup_restore.md) 已落地（待补恢复演练记录）
- [x] AI 降级模式可用并完成基础验证
